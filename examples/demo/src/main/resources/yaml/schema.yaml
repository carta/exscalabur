$KEY.project:
  dataType: "string"
  excelType: "string"

$REP.person:
  dataType: "string"
  excelType: "string"

$REP.gpa:
  dataType: "double"
  excelType: "number"

$REP.major:
  dataType: "string"
  excelType: "string"

$REP.school:
  dataType: "string"
  excelType: "string"

$REP.numStudents:
  dataType: "long"
  excelType: "number"

$KEY.value1:
  dataType: "double"
  excelType: "number"

$KEY.value2:
  dataType: "double"
  excelType: "number"

$KEY.value3:
  dataType: "double"
  excelType: "number"

$REC.dateHeader:
  dataType: "string"
  excelType: "string"

$REC.peerCloseHeader:
  dataType: "string"
  excelType: "string"

$REC.peerChangeHeader:
  dataType: "string"
  excelType: "string"

$REC.date:
  dataType: "date"
  excelType: "date"

$REC.peerClose:
  dataType: "double"
  excelType: "number"

$REC.peerChange:
  dataType: "double"
  excelType: "number"